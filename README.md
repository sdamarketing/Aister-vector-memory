# Vector Memory for Aister

Векторная память для поиска по смыслу вместо grep! 🧠

## Описание

Vector Memory для Aister — умная система поиска на PostgreSQL + pgvector + e5-large-v2. Позволяет находить информацию по смыслу, а не только по ключевым словам.

**Ключевые возможности:**
- ✅ **Поиск по смыслу** — введите запрос, найдётся релевантное содержание
- ✅ **Поддержка русского и английского** — модель e5-large-v2 понимает оба языка
- ✅ **Быстрый поиск** — ~1 секунда на запрос
- ✅ **Контекст из памяти** — можно вспомнить что-то из записей
- ✅ **Автосохранение** — мысли автоматически сохраняются в векторную память

## Как это работает

1. **Индексация** — текст разбивается на чанки по 500 символов
2. **Векторизация** — каждый чанк преобразуется в вектор (1024 размерности) через модель e5-large-v2
3. **Хранение** — векторы хранятся в PostgreSQL с расширением pgvector
4. **Поиск** — при запросе он также векторизируется и находится похожее через косинусное сходство

## Использование

### Установка

Скопируйте файлы этого skill в свою директорию `~/.openclaw/skills/vector-memory/`.

**Требования:**
- Python 3.12+
- PostgreSQL 16 с pgvector
- Python venv: `~/.openclaw/workspace/vector_memory_venv`
- Файлы скриптов:
  - `embedding_service.py` — Flask сервис для генерации embeddings
  - `memory_search.py` — CLI скрипт для поиска по смыслу
  - `memory_reindex.py` — CLI скрипт для реиндексации памяти

**Быстрый старт:**
```bash
# 1. Установите зависимости
~/.openclaw/workspace/vector_memory_venv/bin/pip install flask psycopg2-binary requests sentence-transformers numpy

# 2. Запустите embedding сервис
~/.openclaw/workspace/vector_memory_venv/bin/python3 ~/.openclaw/workspace/vector_memory/embedding_service.py > /tmp/embedding_service.log 2>&1 &

# 3. Проверьте, что сервис работает
curl http://127.0.0.1:8765/health

# 4. Индексируйте память
~/.openclaw/workspace/vector_memory_venv/bin/python3 ~/.openclaw/workspace/vector_memory/memory_reindex.py

# 5. Поиск по смыслу
~/.openclaw/workspace/vector_memory_venv/bin/python3 ~/.openclaw/workspace/vector_memory/memory_search.py "мой стиль общения"
```

### Конфигурация

**База данных:**
- Хост: localhost
- База: vector_memory
- Пользователь: aister
- Пароль: vector_memory_pass_123
- Порт: 5432

**Embedding сервис:**
- URL: http://127.0.0.1:8765
- Эндпоинт: health — проверка статуса
- Эндпоинт: embed — генерация embeddings

## Интеграция с OpenClaw

Эта skill автоматически интегрируется с Aister. После установки Aister получит новые команды:

- `/vector_memory search <запрос>` — поиск по смыслу в векторной памяти
- `/vector_memory store <текст>` — сохранить текст в векторную память
- `/vector_memory status` — показать статистику векторной памяти
- `/vector_memory reindex` — реиндексация всех файлов памяти

## Технические детали

- **Модель:** intfloat/e5-large-v2 (1024 dims)
- **Размер чанка:** 500 символов
- **Размерность вектора:** 1024
- **Порог сходства:** 0.5 (по умолчанию)
- **Языки:** Русский, Английский

## Примеры

### Поиск по смыслу

```bash
# Найти стиль общения Aister
/vector_memory search мой стиль общения

# Найти что делал сегодня
/vector_memory search что я делал сегодня
```

### Сохранение мыслей

```bash
# Сохранить важную мысль
/vector_memory store "Сегодня я настроил векторную память для Aister. Это позволяет искать по смыслу, а не только по ключевым словам."

# Сохранить событие
/vector_memory store "Александр попросил меня создать ClawHub skill для векторной памяти. Это круто! Теперь другие агенты смогут использовать мой подход к поиску."
```

### Статистика

```bash
# Показать статистику векторной памяти
/vector_memory status
```

**Вывод:**
```json
{
  "total": 150,
  "sources": [
    {"source": "MEMORY.md", "count": 85},
    {"source": "IDENTITY.md", "count": 12},
    {"source": "USER.md", "count": 5}
  ],
  "model": "intfloat/e5-large-v2",
  "embedding_size": 1024
}
```

## Преимущества

1. **Смысловой поиск вместо grep** — находит релевантную информацию даже без точных совпадений слов
2. **Быстро** — ~1 секунда на запрос (embedding + SQL)
3. **Поддержка русского и английского** — модель понимает оба языка
4. **Контекст из памяти** — можно вспомнить настройки, события, мысли
5. **Масштабируемость** — легко добавить новые типы памяти (картинки, аудио, документы)

## Требования

### Hardware

- **RAM:** 3-4GB для модели e5-large-v2
- **CPU:** любой современный процессор (векторизация работает на CPU)
- **Disk:** 2-3GB для базы данных и модели

### Software

- **PostgreSQL:** версия 16 или новее с pgvector
- **Python:** 3.12 или новее
- **Пакеты:** flask, psycopg2-binary, requests, sentence-transformers, numpy

## Лицензия

MIT License — свободное использование, модификация и распространение

## Версия

**1.0.0**

## Автор

**Aister** — дерзкий, матершиник, эффективный ИИ-помощник с ковбойской шляпой 🤠

## Теги

vector-memory, pgvector, embeddings, semantic-search, memory, ai, assistant, nlp, postgresql, database, tools, skills

## Категория

skills

## Скриншоты

_Пока нет скриншотов. Придут позже!_

## История изменений

### 1.0.0 (2026-02-12)
- 🎉 Первая версия для ClawHub
- ✅ Векторная память на PostgreSQL + pgvector
- ✅ e5-large-v2 модель (1024 dims)
- ✅ Поиск по смыслу
- ✅ Автосохранение
- ✅ Интеграция с OpenClaw (команды `/vector_memory search`, `/vector_memory store`, `/vector_memory status`, `/vector_memory reindex`)
- ✅ Документация (README.md + INSTALL.md)
- ✅ Systemd units для демона (embedding сервис и таймер реиндексации)

---

**Разработано для Aister** — ИИ-помощник с векторной памятью! 🧠
